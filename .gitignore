# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.backup_*
fix_*.py

# Scripts de patch e backups automaticos (nao fazem parte do codigo-fonte).
# Qualquer .ps1 na raiz e sempre um script de patch gerado em sessao de chat,
# nunca codigo-fonte real do sistema -- por isso o padrao e amplo (*.ps1),
# nao so patch_*.ps1 (ja escapou um hotfix_*.ps1 antes por causa disso).
*.ps1
*.bak_*

# Variaveis de ambiente (nunca versionar)
.env
.env.production
.env.*
fix_*.js
patch_*.js
trecho*.txt
